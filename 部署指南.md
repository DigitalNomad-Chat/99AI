# 99AI 项目部署指南

## 📋 项目概览

| 模块 | 技术栈 | 端口 | 说明 |
|------|--------|------|------|
| **chat** | Vue 3 + Vite | 9002 | 用户对话界面 |
| **admin** | Vue 3 + Element Plus | 9000 | 管理后台 |
| **service** | NestJS + MySQL + Redis | 9520 | 后端 API 服务 |

---

## 🚀 一、开发部署（本地开发环境）

### 前置要求

```bash
# Node.js 版本要求
node: ^18.18.0 || ^20.9.0 || >=21.1.0

# 包管理器
pnpm: >= 8.0.0
```

### 1. 安装依赖服务

**方式一：使用 Docker（推荐）**

```bash
# 启动 MySQL 和 Redis
cd AIWebQuickDeploy
docker-compose up -d mysql redis
```

**方式二：本地安装**

```bash
# macOS
brew install mysql redis
brew services start mysql
brew services start redis

# 创建数据库
mysql -u root -p
CREATE DATABASE chatgpt CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 配置环境变量

```bash
# 后端环境变量
cd service
cp .env.example .env
```

编辑 `.env` 文件：

```bash
PORT=9520

# MySQL（根据实际配置修改）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=你的密码
DB_DATABASE=chatgpt

# Redis
REDIS_PORT=6379
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=

# 开发环境标识
ISDEV=true

ADMIN_SERVE_ROOT=/admin
```

### 3. 安装依赖并启动开发服务

```bash
# 终端1：启动后端服务
cd service
pnpm install
pnpm dev          # 监听端口 9520

# 终端2：启动 Chat 前端
cd chat
pnpm install
pnpm dev          # 监听端口 9002

# 终端3：启动 Admin 管理后台
cd admin
pnpm install
pnpm dev          # 监听端口 9000
```

### 4. 访问地址

| 服务 | 地址 | 默认账号 |
|------|------|----------|
| Chat 界面 | http://localhost:9002 | - |
| Admin 管理后台 | http://localhost:9000/admin | super / 123456 |
| API 文档 (Swagger) | http://localhost:9520/api/docs | - |

---

## 🏠 二、生产部署（Docker Compose）

### 部署架构

```
┌─────────────────────────────────────────────────┐
│                   Nginx 反向代理                  │
│              (可选，用于 SSL 和域名)              │
└─────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  99ai 容器    │ │   MySQL 容器  │ │   Redis 容器  │
│  (端口 9520) │ │  (端口 3306) │ │  (端口 6379) │
│              │ │              │ │              │
│  - NestJS    │ │  - 数据持久化 │ │  - 缓存持久化 │
│  - Chat 静态 │ │  - 数据卷挂载 │ │  - 数据卷挂载 │
│  - Admin 静态│ │              │ │              │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 步骤 1：构建项目

```bash
# 执行构建脚本
chmod +x build.sh
./build.sh
```

构建产物将输出到 `AIWebQuickDeploy/` 目录：

```
AIWebQuickDeploy/
├── dist/              # 后端编译产物
├── public/
│   ├── admin/         # 管理后台静态文件
│   └── chat/          # Chat 静态文件
├── docker-compose.yml
├── Dockerfile
└── .env.docker
```

### 步骤 2：配置生产环境变量

```bash
cd AIWebQuickDeploy
```

**⚠️ 重要：修改默认密码！**

编辑 `docker-compose.yml`：

```yaml
environment:
  MYSQL_ROOT_PASSWORD: '你的强密码'      # 修改！
  MYSQL_PASSWORD: '你的强密码'           # 修改！
```

编辑 `.env.docker`：

```bash
DB_PASS=你的强密码
```

### 步骤 3：启动服务

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f 99ai

# 查看服务状态
docker-compose ps
```

### 步骤 4：验证部署

```bash
# 检查服务健康状态
curl http://localhost:9520/api/health

# 访问服务
# Chat 界面: http://localhost:9520
# Admin 管理后台: http://localhost:9520/admin
```

### 步骤 5：初始化数据

首次启动后，数据库会自动初始化表结构。使用默认管理员账号登录：

| 用户名 | 密码 | 权限 |
|--------|------|------|
| super | 123456 | 超级管理员 |
| admin | 123456 | 普通管理员（需激活） |

**⚠️ 生产环境请立即修改默认密码！**

---

## 🔒 三、生产环境优化配置

### 1. 配置域名和 HTTPS（推荐）

使用 Nginx 反向代理：

```nginx
# /etc/nginx/sites-available/99ai
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # Chat 界面
    location / {
        proxy_pass http://127.0.0.1:9520;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API 接口
    location /api {
        proxy_pass http://127.0.0.1:9520/api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 管理后台
    location /admin {
        proxy_pass http://127.0.0.1:9520/admin;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 2. 前端 API 地址配置

**重要：部署到域名时需要修改前端配置**

编辑 `chat/.env.production` 和 `admin/.env.production`：

```bash
# chat/.env.production
VITE_GLOB_API_URL=https://your-domain.com/api

# admin/.env.production
VITE_APP_API_BASEURL=https://your-domain.com/api
```

然后重新执行 `./build.sh` 构建。

### 3. 数据持久化

当前 Docker Compose 已配置数据卷：

```yaml
volumes:
  - ./data/mysql/:/var/lib/mysql/    # MySQL 数据
  - ./data/redis/:/data/             # Redis 数据
  - myapp_data:/app/data             # 应用实例数据
```

### 4. 资源限制

编辑 `docker-compose.yml` 添加资源限制：

```yaml
services:
  99ai:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### 5. 日志管理

```yaml
services:
  99ai:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

---

## 🛠️ 四、非 Docker 部署（PM2）

如果不想使用 Docker，可以使用 PM2 直接部署：

```bash
# 1. 构建项目
./build.sh

cd AIWebQuickDeploy

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 配置数据库连接

# 4. 启动服务
pnpm start

# 5. 保存 PM2 配置（开机自启）
pm2 save
pm2 startup

# 常用命令
pm2 list          # 查看进程
pm2 logs 99AI     # 查看日志
pm2 restart all   # 重启
pm2 stop all      # 停止
```

### 关于启动命令的说明

**`pnpm start` 和 `pm2 start pm2.conf.json` 的关系：**

项目在 `package.json` 中配置了启动脚本：
```json
"scripts": {
  "start": "pm2 start pm2.conf.json"
}
```

因此，以下两种方式是**完全等价**的：

| 你的命令 | 实际执行的命令 |
|----------|----------------|
| `pnpm start` | `pm2 start pm2.conf.json` |

**推荐使用 `pnpm start`**，因为：
- ✅ 更简单好记
- ✅ 符合 npm 标准规范
- ✅ 团队协作时统一命令
- ✅ 官方文档推荐方式

**后续管理命令都用 PM2：**

```bash
pm2 list          # 查看运行状态
pm2 logs 99AI     # 实时查看日志
pm2 restart 99AI  # 重启服务
pm2 stop 99AI     # 停止服务
pm2 delete 99AI   # 删除进程
pm2 monit         # 监控面板
```

---

## 📊 五、Docker 常用运维命令

```bash
# 启动服务
docker-compose up -d

# 停止服务
docker-compose down

# 重启服务
docker-compose restart 99ai

# 查看日志
docker-compose logs -f 99ai

# 进入容器
docker-compose exec 99ai bash

# 重新构建镜像
docker-compose build --no-cache 99ai

# 清理未使用的镜像和容器
docker system prune -a

# 备份数据库
docker-compose exec mysql mysqldump -u root -p chatgpt > backup.sql

# 恢复数据库
docker-compose exec -T mysql mysql -u root -p chatgpt < backup.sql
```

---

## 📝 六、部署检查清单

### 开发环境

- [ ] Node.js 版本符合要求
- [ ] 已安装 pnpm
- [ ] MySQL/Redis 已启动
- [ ] 环境变量已配置
- [ ] 依赖已安装
- [ ] 后端服务正常启动（9520）
- [ ] Chat 前端正常启动（9002）
- [ ] Admin 前端正常启动（9000）

### 生产环境

- [ ] 已修改所有默认密码
- [ ] 前端 API URL 已配置为生产域名
- [ ] 执行 `./build.sh` 构建项目
- [ ] Docker 或 Docker Compose 已安装
- [ ] 数据持久化卷已配置
- [ ] HTTPS/SSL 已配置（如需要）
- [ ] 防火墙端口已开放
- [ ] 服务健康检查通过
- [ ] 默认管理员密码已修改
- [ ] 数据备份策略已制定

---

## 🔧 七、常见问题

### Q1: 启动后无法连接数据库

**A:** 检查 Docker 容器网络，确保 `.env.docker` 中的 `DB_HOST=mysql` 和 `REDIS_HOST=redis`（使用容器名称）

### Q2: 前端请求 API 跨域

**A:** 生产环境前端和后端部署在同一域名下，使用相对路径 `/api` 即可

### Q3: 文件上传失败

**A:** 确保 `public/file` 目录有写入权限

```bash
chmod 777 /path/to/AIWebQuickDeploy/public/file
```

### Q4: 内存不足

**A:** 调整 `pm2.conf.json` 中的 `max_memory_restart` 或 Docker 容器内存限制

### Q5: pnpm 安装失败

**A:** 确保已安装 pnpm，项目强制使用 pnpm 作为包管理器

```bash
npm install -g pnpm
```

---

## 📚 附录

### 环境变量完整说明

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| PORT | 服务端口 | 9520 |
| DB_HOST | MySQL 主机 | 127.0.0.1 |
| DB_PORT | MySQL 端口 | 3306 |
| DB_USER | MySQL 用户名 | root |
| DB_PASS | MySQL 密码 | - |
| DB_DATABASE | 数据库名 | chatgpt |
| REDIS_HOST | Redis 主机 | 127.0.0.1 |
| REDIS_PORT | Redis 端口 | 6379 |
| REDIS_PASSWORD | Redis 密码 | - |
| ISDEV | 是否测试环境 | false |
| ADMIN_SERVE_ROOT | 管理后台路径 | /admin |

### 目录结构说明

```
99AI-4.3.0/
├── admin/              # 管理后台前端
├── chat/               # 用户对话界面前端
├── service/            # 后端 API 服务
├── AIWebQuickDeploy/   # 部署目录（构建产物）
├── build.sh            # 构建脚本
└── 部署指南.md         # 本文档
```
